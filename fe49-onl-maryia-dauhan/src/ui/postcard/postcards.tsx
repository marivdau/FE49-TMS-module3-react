import { ReactElement } from "react";
import { Postcard } from "./postcard";

type Props = {
  children: ReactElement[]
}

export const Postcards: React.FC<Props> = ({ children }) => {
  return (
    <div>

    </div>
  )
}
